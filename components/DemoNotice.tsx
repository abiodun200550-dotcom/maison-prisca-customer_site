'use client';
import { useEffect,useState } from 'react';
export default function DemoNotice(){const [ref,setRef]=useState('');useEffect(()=>{const params=new URLSearchParams(window.location.search);const fromUrl=params.get('ref');let stored='';try{stored=window.sessionStorage.getItem('maison-prisca-ref')||'';if(fromUrl)window.sessionStorage.setItem('maison-prisca-ref',fromUrl);}catch{}setRef(fromUrl||stored);},[]);const name=ref?ref.replace(/[-_]+/g,' ').replace(/\b\w/g,c=>c.toUpperCase()):'';return <div className="demo-notice"><span>DEMO STORE</span><p>{name?<>Imagine this ordering experience with <strong>{name}&apos;s</strong> collection.</>:<>A sample storefront showing how your fashion brand could sell online.</>}</p><a href="/#contact">Want this customized for your brand?</a></div>;}
